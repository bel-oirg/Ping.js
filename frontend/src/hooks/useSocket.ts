// This hook is deprecated - SocketContext now handles socket initialization
// Kept for backward compatibility but does nothing
export function useSocket(token: string | null, isAuthenticated: boolean) {
  // Socket initialization is now handled by SocketContext
  // This hook is kept only for backward compatibility
} 