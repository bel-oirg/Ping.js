import pool from '../config/pooling.js'

export default {
    async buy(accoundID, item_id, item_type) {
        const check = await pool.query('')
        //check the price

        avatar_list = pool.query('SELECT')

    },
}